/**
 * A simple label/tag to show which inputs are going to be shown on web
 */
import styles from './Web.module.scss'

const Web = () => {
  return <div className={styles.tag}>web</div>
}

export default Web
